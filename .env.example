# Analytics — optional. Without UMAMI_WEBSITE_ID the build emits no script tag
# at all. Read at build time by vite.config.ts, never exposed to the bundle.
UMAMI_WEBSITE_ID=
# Self-hosted instance. Omit to use Umami Cloud (https://cloud.umami.is/script.js).
UMAMI_SCRIPT_URL=
